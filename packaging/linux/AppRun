#!/bin/bash
# AppImage entry point — sets up library path and launches the app.
HERE="$(dirname "$(readlink -f "$0")")"
export LD_LIBRARY_PATH="${HERE}/usr/lib:${LD_LIBRARY_PATH}"
exec "${HERE}/usr/bin/r1control" "$@"
